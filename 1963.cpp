#include <string>

using namespace std;

class Solution {
public:
  int minSwaps(string s) {
    int res = 0;
    int n = s.size();
    int l = 0, r = n - 1;

    while (l <= r) {
      // if s[l] != s[r] oposite res++ and move pointers
    }

    return res;
  }
};
